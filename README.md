# Tests

## Backend (Node/Express)

Depuis `backend/` :

```powershell
cd backend
npm test
```

Tests disponibles :
- **Unitaires** : `npm run test:unit`
- **Intégration (HTTP, Supertest)** : `npm run test:int`

# Here are your Instructions
